from django.db import models


CURRENCY_CHOICES = (
        ('R', 'Rupees'),
        ('$', 'Doller'),
        ('E', 'Euro'),
        )
DISCOUNT_TYPE_CHOICES = (
        ('rupees', 'Rupees'),
        ('percentage', 'Percentage')
        )
